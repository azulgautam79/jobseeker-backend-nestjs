FROM node:22-alpine

WORKDIR /app

# Install dependencies (including devDependencies)
COPY package*.json ./

RUN npm ci

# Copy source (will be overridden by the bind mount)
COPY . .

ENV NODE_ENV=development

EXPOSE 7000

CMD ["npm", "run", "start:dev"]